# 2026-03-19

## 给女朋友创建 GF Agent

- **目标**：创建一个只能 SSH 的独立 agent，让她通过 Telegram 新 bot 操控自己的 Mac
- **踩坑记录**：
  1. 第一次给的 bot token 竟然是原来 @myglm_bot 的，导致主 bot 被 deleteWebhook + setMyCommands 搞坏
  2. agents.list 里只放 gf 没有 main，导致所有消息路由到 gf
  3. 多次 config.patch 热重载导致主 Telegram channel 完全不启动
  4. 解决方案：恢复备份 → 重新正确配置
- **最终配置**：
  - gf agent: tools.allow=["exec"], exec.security=allowlist, safeBins=["ssh"]
  - 新 bot: @Everlin_herbot (token: 8524561471:...)
  - 账户: channels.telegram.accounts.gf
  - 绑定: gf accountId → gf agent
  - allowFrom: ["8610321223"] (@wannysu)
  - 独立 workspace: /home/useradmin/.openclaw/workspace-gf/
- **待办**：SSH 连接她 Mac（需要网络方案 + 密钥配置）

## Telegram 主 Bot 修复

- 22:44 左右发现 @myglm_bot 不回消息
- 原因：配置格式未迁移，顶层旧值没移到 `accounts.default`，导致默认 bot 不启动
- 解决：`openclaw doctor --non-interactive --fix` 迁移配置 + 热重载
- 备份：`openclaw.json.bak`
- gf bot（@Everlin_herbot）不受影响

## Mamba-3 Paper Review（subagent 超时）

- subagent 2m40s 超时，只返回搜索结果没有 review
- TODO: 你如果要的话可以重试
