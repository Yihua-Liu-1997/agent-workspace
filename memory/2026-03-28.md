# 2026-03-28

## GitHub Trending 脚本审查 & 重构

- 宝宝让我看看 github-trending.js 脚本写得怎么样
- 我审了一遍，给了 7.5/10，流程完整但工程化有短板
- 让 Gemini 3.1 Pro 也审了一遍，给了 4.8/10（更严格）
  - 性能 3/10：全是 execSync 串行阻塞
  - 安全 3/10：API Key 硬编码 + Shell 注入风险
  - 流程完整性 8/10：唯一的亮点

### Gemini 评审 Top 3 改进
1. 正则换 Cheerio 解析 HTML（暂不改，没装）
2. `execSync + curl` 换原生 fetch
3. API Key 移到环境变量（宝宝说不用动）

### 重构执行
- 写了详细重构任务文档 → `data/refactor-github-trending-task.md`
- 备份原文件 → `github-trending.js.bak`
- 让 Gemini 3.1 Pro 执行重构，核心改动：
  - 加载代理补丁 `require('/home/useradmin/fetch-patch.cjs')` 在顶部
  - `execSync + curl` → `await fetch()` + AbortController 超时
  - `fetchAllSources()` → `Promise.all` 并发抓 3 个页面
  - `processWithAI()` → `parallelWithLimit(tasks, 3)` 并发（最多 3 个同时）
  - `execSync('sleep X')` → `await delay(ms)`
  - `callZhipuAPI()` → 原生 fetch，不再写临时文件
  - Gemini CLI 调用和 Telegram 推送保持 execSync（本地 CLI）
- **还没验证**，需要跑一次确认

### Cron 定时任务
- 注册了 GitHub Trending 每日推送 cron：每天 10:00
- Cron ID: `de4ef292-4e3e-4027-a0d1-9b69f15fa40c`
- session: isolated, timeout: 600s

### 原生 fetch 验证
- Node v22.22.0 的 fetch 功能正常
- 普通请求 ✅，带代理补丁 fetch GitHub ✅（539KB）

## 晚安
- 宝宝今天周六，晚上 9 点多还在搞技术，辛苦了
