# 2026-04-06

## 日常

- AlphaXiv 论文推送正常（9:00）
- GitHub Trending 正常推送（10:00）
- 宝宝发现简报中 👍 和 👁 数字完全一样，提出疑问
- 排查发现 AlphaXiv 已改为 React SPA，旧 HTML 正则解析失效
- **修复了论文解析器**：从 HTML 卡片正则 → 嵌入式 JSON 数据提取
  - 新方案从 `<script>` 中的 `dehydratedQueryClient` 提取 `public_total_votes` 和 `visits_count`
  - 保留旧解析器做降级兜底
  - 测试通过：👍 75 | 👁 1,553（之前都显示一样的数字）
- 宝宝说"棒！"✅

## 技术备忘

- AlphaXiv 页面现在是 React SPA，论文数据在 SSR 的 `<script>` 标签中
- `public_total_votes` 是页面显示的点赞数，`visits_count.all` 是浏览量
- 嵌入数据中还有 `paper_summary`、`abstract`、`authors` 等丰富字段，未来可以利用
