I will first locate and read the PDF file for the paper to understand its content. I'll also try fetching the arXiv page for additional metadata if needed.
I will use a shell command to fetch the paper's metadata from arXiv and extract text from the local PDF file, as it is outside the standard workspace directories.
I will write a Python script to extract the text from the local PDF file since `pdftotext` is not available.
I will copy the PDF file into the allowed workspace directory so I can use the dedicated file reading tool to extract its contents.
I will read the copied PDF file using the dedicated file reading tool to analyze its content.